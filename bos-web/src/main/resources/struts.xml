<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
    "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
	<!-- 开发模式 -->
	<constant name="struts.devMode" value="false"></constant>
	<!-- 默认在Struts2整合Spring的包中已经开启 <constant name="struts.objectFactory" value="spring"/> -->
	<package name="basicbos" namespace="/" extends="struts-default">
		<!-- 配置拦截器 -->
		<interceptors>
			<interceptor name="BOSLoginInterceptor" class="cn.itcast.bos.web.interceptor.BOSLoginInterceptor">
				<!-- 指定哪些方法不需要拦截 -->
				<param name="excludeMethods">login</param>
			</interceptor>
			<!-- 定义拦截器栈 -->
			<interceptor-stack name="BOSInterceptorStack">
				<interceptor-ref name="BOSLoginInterceptor"/>
				<interceptor-ref name="defaultStack"/>
			</interceptor-stack>
		</interceptors>
		<!-- 设置拦截器栈 -->
		<default-interceptor-ref name="BOSInterceptorStack"/>
		<!-- 全局结果集 -->
		<global-results>
			<result name="login">/login.jsp</result>
		</global-results>
		<!-- 访问权限控制 -->
		<action name="page_*_*">
			<result type="dispatcher">/WEB-INF/pages/{1}/{2}.jsp</result>
		</action>
		<!-- 用户管理 -->
		<action name="userAction_*" class="userAction" method="{1}">
			<result name="home" type="redirect">/index.jsp</result>
		</action>
		<!-- 取派员管理 -->
		<action name="staffAction_*" class="staffAction" method="{1}">
			<result name="list">/WEB-INF/pages/base/staff.jsp</result>
		</action>
		<!-- 区域管理 -->
		<action name="regionAction_*" class="regionAction" method="{1}">
			<result name="list">/WEB-INF/pages/base/region.jsp</result>
		</action>
		<!-- 分区管理 -->
		<action name="subareaAction_*" class="subareaAction" method="{1}">
			<result name="list">/WEB-INF/pages/base/subarea.jsp</result>
		</action>
		<!-- 定区管理 -->
		<action name="decidedzoneAction_*" class="decidedzoneAction" method="{1}">
			<result name="list">/WEB-INF/pages/base/decidedzone.jsp</result>
		</action>
		<!-- 工作通知单管理 -->
		<action name="noticebillAction_*" class="noticebillAction" method="{1}">
			<result name="noticebill_add">/WEB-INF/pages/qupai/noticebill_add.jsp</result>
		</action>
	</package>
</struts>